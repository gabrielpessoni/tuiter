<div class="home-header">
    <h1>Feed</h1>
</div>

<div class="tuits-container">
    {{#each tuits}}
    <figure>
        <blockquote>
            "{{this.title}}"
        </blockquote>

        <figcaption>por <span>{{this.User.name}}</span></figcaption>
    </figure>
    {{/each}}
</div>